# Button
> Form 下 vButton组件

```js
  import Button from 'VForm/Button'

  export default defineComponent({
      name : 'demo'
      render(){
        return (
           <Button type='primary' text='Demo Button'/>
        )
      }
  })

```